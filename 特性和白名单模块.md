1特性

1.1生命特性 (Life Characteristics)

**1. 功能定义 (Core Functionality)**

- **机制**：**伤害免疫 (Damage Immunity)**。
- **逻辑**：当特性处于**激活 (Active)** 状态时，拦截并取消 (Cancel) 针对该玩家的所有伤害事件 (Damage Events)，实现无敌效果。

**2. 交互与控制 (Interaction & Controls)**

- **状态切换 (Toggle)**：支持通过以下两种方式切换“开启/关闭”状态：
  1.  **键位绑定 (Key Binding)**：默认绑定为 `Keypad 1` (小键盘 1)固定，不可更改。按下时向服务端发送数据包请求切换状态。
  2.  **命令控制 (Commands)**：
      - 开启：`/wenxingtools lifeCharacteristics on`
      - 关闭：`/wenxingtools lifeCharacteristics off`

**3. 权限系统 (Permission System)**

- **鉴权逻辑 (Access Control)**：玩家满足以下**任意一项 (OR Logic)** 条件即可操作：
  - `AllowList`：玩家在模组配置的白名单中。
  - `Creative Mode`：玩家处于创造模式。
  - `Op/Cheats`：玩家拥有作弊权限（单人开启作弊 或 服务器 OP 等级 ≥ 2）。
- **失败反馈**：若权限验证失败，**不执行任何操作且不返回任何提示 (Silent Fail)**。

**4. 用户反馈 (Feedback UI)**

- **提示位置**：**操作栏 (Action Bar)**。
  - _注：即物品栏上方显示的临时文本，不同于聊天栏 (Chat) 或 标题 (Title)。_
- **提示内容**：仅在状态**变更成功**时显示提示（例如：“生命特性：已开启”）。

  1.2攻击特性 (Attack Characteristics)

**1. 功能定义 (Core Functionality)**

- **机制**：**强制一击必杀 (Forced One-Hit Kill)**。
- **逻辑**：当特性处于**激活 (Active)** 状态时，监听玩家造成的伤害事件。
  - **步骤一（常规致死）**：将伤害数值修改为 `Float.MAX_VALUE`，确保触发正常的死亡流程（掉落物、经验值、击杀统计）。
  - **步骤二（突破锁血）**：同步将目标实体的生命值强制设为 `0` (`setHealth(0)`)。
    - _目的_：绕过部分模组生物或 Boss 存在的“单次受击伤害上限（Damage Cap/Invulnerability）”机制，实现“众生平等”的秒杀效果。

**2. 交互与控制 (Interaction & Controls)**

- **状态切换 (Toggle)**：支持通过以下两种方式切换“开启/关闭”状态：
  1.  **键位绑定 (Key Binding)**：默认绑定为 `Keypad 2` (小键盘 2)固定，不可更改。按下时向服务端发送数据包请求切换状态。
  2.  **命令控制 (Commands)**：
      - 开启：`/wenxingtools attackCharacteristics on`
      - 关闭：`/wenxingtools attackCharacteristics off`

**3. 权限系统 (Permission System)**

- **鉴权逻辑 (Access Control)**：玩家满足以下**任意一项 (OR Logic)** 条件即可操作：
  - `AllowList`：玩家在模组配置的白名单中。
  - `Creative Mode`：玩家处于创造模式。
  - `Op/Cheats`：玩家拥有作弊权限（单人开启作弊 或 服务器 OP 等级 ≥ 2）。
- **失败反馈**：若权限验证失败，**不执行任何操作且不返回任何提示 (Silent Fail)**。

**4. 用户反馈 (Feedback UI)**

- **提示位置**：**操作栏 (Action Bar)**。
- **提示内容**：仅在状态**变更成功**时显示提示（例如：“攻击特性：已开启”）。

  1.3资源特性 (Resource Characteristics)

**1. 功能定义 (Core Functionality)**

- **机制**：**物品丢弃倍增 (Item Toss Multiplication)**。
- **逻辑**：当特性处于**激活 (Active)** 状态时，监听玩家的物品丢弃事件 (`ItemTossEvent`)。
  - **计算公式**：`最终数量 = 原始数量 × 倍率因子 (Multiplier)`。
  - **实现方式**：直接修改生成的物品实体 (`ItemEntity`) 堆叠数量 (`ItemStack count`)，**不设上限** (No Cap)。
  - **默认倍率**：`10`。

**2. 参数配置 (Configuration)**

- **倍率修改**：
  - 命令：`/wenxingtools setResourceCharacteristics <amount>`
  - `<amount>`：整数型参数，用于设置上述公式中的“倍率因子”。
  - **成功反馈**：设置成功后，通过**操作栏 (Action Bar)** 显示提示（例如：“资源特性倍率：已设置为 10”）。

**3. 交互与控制 (Interaction & Controls)**

- **状态切换 (Toggle)**：支持通过以下两种方式切换“开启/关闭”状态：
  1.  **键位绑定 (Key Binding)**：默认绑定为 `Keypad 3` (小键盘 3)固定，不可更改。按下时向服务端发送数据包请求切换状态。
  2.  **命令控制 (Commands)**：
      - 开启：`/wenxingtools resourceCharacteristics on`
      - 关闭：`/wenxingtools resourceCharacteristics off`

**4. 权限系统 (Permission System)**

- **鉴权逻辑 (Access Control)**：玩家满足以下**任意一项 (OR Logic)** 条件即可操作：
  - `AllowList`：玩家在模组配置的白名单中。
  - `Creative Mode`：玩家处于创造模式。
  - `Op/Cheats`：玩家拥有作弊权限（单人开启作弊 或 服务器 OP 等级 ≥ 2）。
- **失败反馈**：若权限验证失败，**不执行任何操作且不返回任何提示 (Silent Fail)**。

**5. 用户反馈 (Feedback UI)**

- **提示位置**：**操作栏 (Action Bar)**。
- **提示内容**：仅在状态**变更成功**时显示提示（例如：“资源特性：已开启 (倍率: 10)”）。

  1.4自由特性 (Freedom Characteristics)

**1. 功能定义 (Core Functionality)**

- **机制**：**创造模式飞行能力 (Creative Flight Capability)**。
- **逻辑**：
  - **激活 (Active)**：赋予玩家 `mayFly` 能力，允许在生存模式下进行创造式飞行（双击空格起飞）。
  - **关闭 (Inactive)**：移除 `mayFly` 能力。若玩家正在飞行中，应取消飞行状态 (`flying = false`) 并使其受重力影响落下（除非玩家本身处于创造/旁观模式）。

**2. 交互与控制 (Interaction & Controls)**

- **状态切换 (Toggle)**：支持通过以下两种方式切换“开启/关闭”状态：
  1.  **键位绑定 (Key Binding)**：默认绑定为 `Keypad 4` (小键盘 4)固定，不可更改。按下时向服务端发送数据包请求切换状态。
  2.  **命令控制 (Commands)**：
      - 开启：`/wenxingtools freedomCharacteristics on`
      - 关闭：`/wenxingtools freedomCharacteristics off`

**3. 权限系统 (Permission System)**

- **鉴权逻辑 (Access Control)**：玩家满足以下**任意一项 (OR Logic)** 条件即可操作：
  - `AllowList`：玩家在模组配置的白名单中。
  - `Creative Mode`：玩家处于创造模式。
  - `Op/Cheats`：玩家拥有作弊权限（单人开启作弊 或 服务器 OP 等级 ≥ 2）。
- **失败反馈**：若权限验证失败，**不执行任何操作且不返回任何提示 (Silent Fail)**。

**4. 用户反馈 (Feedback UI)**

- **提示位置**：**操作栏 (Action Bar)**。
- **提示内容**：仅在状态**变更成功**时显示提示（例如：“自由特性：已开启”）。

  1.5时间特性 (Time Characteristics)

**1. 功能定义 (Core Functionality)**

- **机制**：**区块 Tick 倍增 (Chunk Tick Acceleration)**。
- **逻辑**：当特性处于**激活 (Active)** 状态时，将玩家“开启瞬间所在的单个区块”在服务端进行 Tick 倍增处理，实现加速效果。
  - **倍率 N（每玩家独立）**：对该区块的 Tick 额外执行 `N-1` 次。
  - **覆盖范围**：
    - **随机刻 (Random Tick)**：加速农作物生长等依赖随机刻的行为。
    - **方块实体 Tick (BlockEntity Tick)**：加速熔炉烧制、漏斗传输，以及大多数依赖 `BlockEntityTicker` 的机器方块（含部分模组机器）。
  - **实现方式**：
    - 对目标区块额外执行区块 Tick（触发随机刻等区块级逻辑）。
    - 对目标区块内的方块实体，通过 `EntityBlock#getTicker(...)` 获取 `BlockEntityTicker` 并额外调用 `ticker.tick(...)`，实现“类似加速火把（Torcherino）”的工作方块加速效果。
  - **不保证加速**：若某些模组机器不使用 `BlockEntityTicker`/世界 Tick 驱动（例如独立线程/真实时间计时），则可能不会被该机制加速。
  - **多人规则**：同一维度、同一区块内若存在多个开启“时间特性”的玩家，则取该区块内玩家配置的 **最大倍率** 作为实际倍率。
  - **默认倍率**：`100`。
  - **倍率上限**：`10000`。

**2. 参数配置 (Configuration)**

- **倍率修改**：
  - 命令：`/wenxingtools setTimeCharacteristics <amount>`
  - `<amount>`：整数型参数，范围 `1~10000`，每个玩家独立保存。
  - **成功反馈**：设置成功后，通过**操作栏 (Action Bar)** 显示提示（例如：“时间特性倍率：已设置为 100”）。

**3. 交互与控制 (Interaction & Controls)**

- **状态切换 (Toggle)**：支持通过以下两种方式切换“开启/关闭”状态：
  1.  **键位绑定 (Key Binding)**：默认绑定为 `Keypad 5` (小键盘 5)固定，不可更改。按下时向服务端发送数据包请求切换状态。
  2.  **命令控制 (Commands)**：
      - 开启：`/wenxingtools timeCharacteristics on`
      - 关闭：`/wenxingtools timeCharacteristics off`

**4. 自动关闭 (Auto Disable)**

- **切换区块**：玩家移动到其他区块时，自动关闭时间特性。
- **切换维度**：玩家切换维度时，自动关闭时间特性。
- **区块未加载**：若玩家所在区块未处于加载状态，自动关闭时间特性。

**5. 权限系统 (Permission System)**

- **鉴权逻辑 (Access Control)**：玩家满足以下**任意一项 (OR Logic)** 条件即可操作：
  - `AllowList`：玩家在模组配置的白名单中。
  - `Creative Mode`：玩家处于创造模式。
  - `Op/Cheats`：玩家拥有作弊权限（单人开启作弊 或 服务器 OP 等级 ≥ 2）。
- **失败反馈**：若权限验证失败，**不执行任何操作且不返回任何提示 (Silent Fail)**。

**6. 用户反馈 (Feedback UI)**

- **提示位置**：**操作栏 (Action Bar)**。
- **提示内容**：仅在状态**变更成功**时显示提示（例如：“时间特性：已开启 (倍率: 100)”；自动关闭会提示关闭原因）。

  1.6附魔指令特性 (Enchantment Command Characteristics)

**1. 功能定义 (Core Functionality)**

- **机制**：**强制写入附魔 (Forced Enchant Write)**。
- **逻辑**：通过命令将指定附魔及等级直接写入目标实体“主手物品”的附魔列表，不进行“物品类型/附魔类别”校验。
- **作用范围**：对 `LivingEntity` 有效（玩家/生物均可）；若目标主手为空则跳过。

**2. 命令接口 (Command Interface)**

- 命令：`/wenxingtools cenchant <targets> <enchantment_id> [level]`
  - `<targets>`：实体选择器（例如 `@s`、`@p`、`@a`、玩家名等）。
  - `<enchantment_id>`：附魔的资源定位符 ID（例如 `minecraft:sharpness`）。
  - `[level]`：可选，默认 `1`，最小值 `1`。

**3. 权限系统 (Permission System)**

- **鉴权逻辑 (Access Control)**：玩家满足以下**任意一项 (OR Logic)** 条件即可使用该指令：
  - `AllowList`：玩家在模组白名单中。
  - `Creative Mode`：玩家处于创造模式。
  - `Op/Cheats`：玩家拥有作弊权限（单人开启作弊 或 服务器 OP 等级 ≥ 2）。
- **失败反馈**：若权限验证失败，命令不可用（无执行入口）。

**4. 用户反馈 (Feedback UI)**

- **提示位置**：聊天栏/系统消息。
- **成功提示**：对已处理的主手物品数量进行汇报，并显示附魔全名与等级。
- **失败提示**：附魔 ID 无效或目标没有可附魔的主手物品时返回失败信息。

  2.2.白名单管理 (Whitelist Management)

**1. 功能定义 (Core Functionality)**

- **机制**：**UUID 访问控制列表 (UUID Access Control List)**。
- **逻辑**：维护一份持久化的玩家 UUID 列表，用于决定谁有权限使用本模组的特性（生命、攻击、资源、自由特性）。
- **扩展**：同样用于“时间特性”的权限验证。
- **存储**：白名单数据应在服务端持久化保存（如保存为 JSON 或 TOML 文件），确保服务器重启后数据不丢失。

**2. 命令接口 (Command Interface)**
提供一组子命令用于管理白名单，基础命令为 `/wenxingtools whitelist`：

- **添加玩家 (Add)**
  - 命令：`/wenxingtools whitelist add <player>`
  - 参数：`<player>` 可以是由于在线玩家名解析出的 `GameProfile` 或直接输入的 UUID。
  - 逻辑：将目标玩家的 UUID 加入白名单。如果已存在，提示“玩家已在白名单中”。

- **移除玩家 (Remove)**
  - 命令：`/wenxingtools whitelist remove <player>`
  - 参数：同上。
  - 逻辑：将目标玩家的 UUID 从白名单移除。如果不存在，提示“玩家不在白名单中”。

- **列表查询 (List)**
  - 命令：`/wenxingtools whitelist list`
  - 逻辑：在聊天栏显示当前白名单内的所有玩家名称（如果能解析）和 UUID。

**3. 权限要求 (Permission Requirement)**

- **管理权限**：与 `/wenxingtools` 根命令保持一致。
  - 玩家满足以下任意一项即可执行白名单管理命令：
    - `AllowList`：玩家在模组白名单中。
    - `Creative Mode`：玩家处于创造模式。
    - `Op/Cheats`：玩家拥有作弊权限（单人开启作弊 或 服务器 OP 等级 ≥ 2）。
