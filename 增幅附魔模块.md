# 增幅附魔模块设计方案

### 一、通用配置

所有三种附魔共享以下通用属性：

| 配置项                                          | 值                                           |
| ----------------------------------------------- | -------------------------------------------- |
| `max_level`（最大等级）                         | 3                                            |
| `weight/rarity`（稀有度）                       | COMMON（普通）                               |
| `anvil_cost / enchanting_cost`（铁砧/附魔成本） | 最低 10 级，最高 15 级                       |
| `conflicts`（冲突）                             | 无冲突，三种附魔可同时存在于同一物品         |
| 获取渠道                                        | 附魔台、村民交易、附魔书均可获取，非宝藏专属 |

#### 1.1 工程实现注意事项

- 指令/脚本批量附魔时，应采用"覆盖写入"的方式设置附魔等级（基于 `EnchantmentHelper.getEnchantments` + `EnchantmentHelper.setEnchantments` 修改附魔映射），避免重复追加同一附魔导致物品 NBT 出现多个相同附魔条目，从而在提示/显示中同时出现多个等级（例如同时显示 III 与 X）。

#### 1.2 AttributeFix 模组联动机制

当检测到游戏安装了 **AttributeFix（模组 ID：`attributefix`）** 模组时，部分附魔参数会自动提升，以适配更高的属性上限环境：

- **检测方式**：通过 `ModList.get().isLoaded("attributefix")` 在运行时动态判定
- **检测时机**：每次触发附魔效果计算时实时检测，支持热插拔环境切换
- **涉及参数**：见下表（详细说明见各对应章节）

| 附魔模块 | 参数项 | 基础值（无 AttributeFix） | 增强值（有 AttributeFix） |
|---------|-------|--------------------------|-------------------------|
| 生命增幅 | 幸运值加成 | +1,024 | +102,400 |
| 生命增幅 | 每级伤害减免比例 | 1% / 级 | 5% / 级 |
| 攻击增幅 | 每级真伤/吸血比例 | 1% / 级 | 5% / 级 |
| 资源增幅 | 生物经验额外倍率 | ×100 | ×10,000 |

- **修饰符自动更新**：若已有修饰符但数值与目标值不一致（例如玩家从无 AttributeFix 环境切换到有 AttributeFix 环境），会先移除旧修饰符再重新添加正确数值的修饰符。

---

### 二、生命增幅（life_amplification）

#### 2.1 基础属性

| 属性                               | 值                              |
| ---------------------------------- | ------------------------------- |
| `applicable_slots`（适用槽位）     | ARMOR（头盔、胸甲、护腿、靴子） |
| `enchantment_category`（附魔分类） | ARMOR                           |

#### 2.2 效果说明

当玩家至少有一件护甲装备了该附魔时，以下所有效果同时生效：

1. **伤害豁免**
   - 效果：减少玩家受到的所有伤害
   - 减免比例：`四件护甲附魔等级总和 × 每级比例`
   - 每级比例：
     - 无 AttributeFix：**1% / 级**
     - 有 AttributeFix：**5% / 级**
   - 示例（四件全 III 级，总等级 12，100 点原始伤害）：
     - 无 AttributeFix：减免 12×1% = 12%，最终伤害 88
     - 有 AttributeFix：减免 12×5% = 60%，最终伤害 40

2. **幸运值提升**
   - 效果：给予玩家幸运属性加成
   - 数值：
     - 无 AttributeFix：**+1,024** 点幸运值
     - 有 AttributeFix：**+102,400** 点幸运值
   - 实现方式：为玩家添加 `Attributes.LUCK` 的属性修饰器（`AttributeModifier.Operation.ADDITION`）；当玩家不再装备该附魔时移除该修饰器
   - 修饰符数值会根据 AttributeFix 检测结果动态同步（检测到数值不匹配时先移除旧修饰符再添加新修饰符）

3. **装备无法破坏**
   - 效果：使玩家所有装备的耐久度不消耗
   - 条件：只要有一件护甲有该附魔，所有装备都获得此效果

4. **无限夜视**
   - 效果：给予玩家永久夜视效果
   - 实现方式：持续应用 `MobEffects.NIGHT_VISION` 效果，持续时间设为 `Integer.MAX_VALUE`；当玩家不再装备该附魔时移除该夜视效果

#### 2.3 生效检测

- 检测时机：持续生效（每 tick 或状态变化时检测）
- 检测范围：玩家装备栏的所有护甲槽位
- 生效条件：至少一件护甲有该附魔
- 等级计算：
  - **触发条件判断**：只要任意一件护甲附有该附魔，即视为生效
  - **伤害减免比例**：取四件护甲上该附魔等级总和，乘以每级比例（基础 1%，有 AttributeFix 时 5%）

---

### 三、攻击增幅（attack_amplification）

#### 3.1 基础属性

| 属性                               | 值           |
| ---------------------------------- | ------------ |
| `applicable_slots`（适用槽位）     | HAND（主手） |
| `enchantment_category`（附魔分类） | WEAPON       |

#### 3.2 效果说明

当玩家作为攻击者造成伤害时，效果触发：

1. **百分比生命扣除**
   - 目标：被攻击的生物
   - 扣除比例：`最大生命值 × 附魔等级 × 每级比例`
   - 每级比例：
     - 无 AttributeFix：**1% / 级**
     - 有 AttributeFix：**5% / 级**
   - 伤害类型：真伤（直接修改生命值，不走护甲/抗性/伤害上限等常规减免）
   - 伤害上限：不超过目标当前生命值（可造成死亡）
   - 示例（目标最大生命值 100）：
     - III 级无 AttributeFix：100 × 3×1% = 3 HP 真伤
     - III 级有 AttributeFix：100 × 3×5% = 15 HP 真伤

2. **生命恢复（吸血）**
   - 目标：玩家
   - 恢复量：等于本次真伤扣除的生命值数值
   - 恢复上限：不超过玩家自身最大生命值

#### 3.3 生效规则

- 触发事件：`LivingHurtEvent`（LOWEST）
- 生效条件：
  - 服务端执行
  - `event.getAmount() > 0`（本次伤害需要实际生效，避免对完全免疫/被取消的攻击也扣血）
  - 伤害源实体为真实玩家（排除 FakePlayer）
- 等级获取：取玩家主手物品的 `attack_amplification` 附魔等级
- 伤害顺序：
  1. 原版伤害流程通过基础判定并产生本次伤害（amount > 0）
  2. 在 `LivingHurtEvent` 末尾执行攻击增幅真伤扣血与吸血
  3. 继续执行原始伤害结算流程
- 适用范围：所有玩家造成的 `LivingDamage` 伤害（包括近战、弓箭、魔法等）

---

### 四、资源增幅（resource_amplification）

#### 4.1 基础属性

| 属性                               | 值           |
| ---------------------------------- | ------------ |
| `applicable_slots`（适用槽位）     | HAND（主手） |
| `enchantment_category`（附魔分类） | DIGGER       |

#### 4.2 放大因子计算

- 基础公式（掉落物倍率、方块掉落、右键收获均适用）：`factor = N × 100`，其中 N = 附魔等级
- 生物经验额外倍率（在 `factor` 基础上再乘以下述倍率）：
  - 无 AttributeFix：**×100**
  - 有 AttributeFix：**×10,000**
- 经验值上限：`RESOURCE_XP_CAP = 2,000,000,000`

#### 4.3 效果一：通用掉落倍增

适用于生物击杀、方块破坏等所有玩家触发的掉落行为。

##### 4.3.1 触发条件

- 来源：真实玩家（排除 FakePlayer）
- 范围：所有生物类型、所有方块类型

##### 4.3.2 掉落物处理

- 计算方式：在原始掉落基础上，追加 `(factor + 1)` 倍的额外掉落
- 示例：原始掉落 2 个，factor=1 → 额外掉落 2×(1+1)=4 个，总计 6 个

##### 4.3.3 经验值处理（仅生物击杀经验）

- 计算方式：`newXp = oldXp × factor × xpMultiplier`
  - `xpMultiplier`：
    - 无 AttributeFix：100
    - 有 AttributeFix：10,000
- 完整公式：
  - 无 AttributeFix：`newXp = oldXp × (等级×100) × 100`
  - 有 AttributeFix：`newXp = oldXp × (等级×100) × 10,000`
- 示例（III 级，原版掉落 5 经验）：
  - 无 AttributeFix：5 × 300 × 100 = **150,000**
  - 有 AttributeFix：5 × 300 × 10,000 = **15,000,000**
- 上限：不超过 2,000,000,000（超过则截断为该值）

##### 4.3.4 实现事件

| 事件                        | 优先级 |
| --------------------------- | ------ |
| `LivingDropsEvent`          | LOWEST |
| `LivingExperienceDropEvent` | LOWEST |
| `BlockEvent.BreakEvent`     | LOWEST |

##### 4.3.5 防重复处理

使用实体 NBT 标记防止同一次事件被重复处理：

- 生物掉落标记：`wenxingtools_resourceAmplificationDropsProcessed`
- 生物经验标记：`wenxingtools_resourceAmplificationXpProcessed`

---

#### 4.4 效果二：右键收获成熟作物

允许玩家右键点击成熟作物来收获，而不需要破坏方块。

##### 4.4.1 触发条件

- 玩家手持有 `resource_amplification` 附魔的工具
- 目标方块具有 `age` 属性
- 目标方块处于成熟状态（age 为最大值）

##### 4.4.2 执行流程

1. 生成该方块的原始掉落物
2. 应用上述"通用掉落倍增"逻辑进行放大
3. 将方块的 `age` 重置为 0
4. 取消默认交互结果（避免重复处理）

##### 4.4.3 实现事件

| 事件                                  | 优先级 |
| ------------------------------------- | ------ |
| `PlayerInteractEvent.RightClickBlock` | LOWEST |
