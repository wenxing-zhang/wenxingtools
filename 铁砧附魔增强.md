# 铁砧附魔增强设计方案

## 1. 目标

- 当前模块聚焦于**铁砧附魔行为增强**。
- 在**生存模式**下，所有玩家都可以通过铁砧将附魔书应用到任意物品。
- 铁砧附魔或等级合并时，经验消耗固定为 **1 级**。
- 保留铁砧附魔等级叠加规则配置：可选“原版规则”或“直接相加”。

## 2. 配置项

- `allowAnyEnchantment`：铁砧是否允许任意物品附魔
- `allowLevelStacking`：铁砧叠加规则（true=相加；false=原版）
- `fixedExperienceCost`：铁砧经验消耗固定值

## 3. Forge 1.20.1 实现要点

### 3.2 铁砧：AnvilMenu#createResult 改造

- 放开“物品适配”校验：当 `allowAnyEnchantment=true` 时，铁砧允许将附魔应用到任意物品。
- 等级合并策略：按 `allowLevelStacking` 计算新等级。
  - `true`：左右附魔等级直接相加
  - `false`：沿用原版铁砧等级合并规则
- 固定经验：将铁砧经验消耗固定为 `fixedExperienceCost`。
- 去除生存模式下原版铁砧的 “Too Expensive!” 限制，确保固定经验模式可正常取出结果。

### 3.3 权限范围

- 本模块不区分管理员、OP 或普通玩家。
- 只要玩家能正常使用铁砧，在生存模式下就默认享有：
  - 任意物品附魔
  - 固定 1 级经验消耗

### 3.4 配置默认值建议

- `allowAnyEnchantment = true`
- `allowLevelStacking = true`
- `fixedExperienceCost = 1`

## 4. 约束与原则

- 多人模式：铁砧结果以服务端计算为准，配置应以服务端为准并同步到客户端。
- 最小化修改：只在“物品适配校验 / 等级合并 / 经验消耗 / 取出限制”这些关键点注入，避免影响原版其它流程。
