MOD名称：wenxingtools

MODID：wenxingtools

MOD作者：wenxing

MOD版本：1.2.4



# 特性与白名单模块 设计方案

> **定位**：本方案为独立模块的核心设计思路规格，仅定义功能语义与行为边界，不涉及任何具体实现技术。本模块可在任意版本项目中独立开发落地，不会对其他模块的开发造成任何干扰或依赖。

---

## 0. 设计范围与独立性声明

### 0.1 设计范围（本模块覆盖什么）

- 玩家特权特性体系：4 种玩家级特权特性的行为语义定义（无敌 / 资源 / 自由 / 杀戮光环）。
- 访问控制权限模型：三种并列权限判定方式的组合规则与失效策略。
- 玩家状态持久化：特性开关与参数在玩家生命周期内的继承与恢复语义。
- 用户控制通道：双通道等价操作（快捷键即时切换 / 命令精确设置）的行为规范。
- 反馈展示规范：不同操作场景下的反馈位置、内容格式与可见范围。
- 白名单管理机制：白名单的存储形态、加载策略、热更新机制与增删查语义。

### 0.2 模块独立性声明

- 本模块是完全自包含的独立单元，不依赖任何其他模块的内部状态或私有接口。
- 本模块暴露的唯一对外契约为「权限判定结果」（布尔值）与「各特性的激活态」，其他模块可选择调用或不调用，本模块不关心调用方。
- 本模块内部的存储、事件、命令、网络通信均为模块内部私有实现，不对外暴露任何内部结构。
- 本模块不读写其他模块的任何持久化数据，不占用其他模块的任何键位、命令、标记位等资源。

---

## 1. 权限模型

### 1.1 设计思路

模组同时面向三种使用场景，三种身份任取其一即可使用本模块功能，不要求满足全部。

| 条件             | 定义                                       |
| ---------------- | ------------------------------------------ |
| ① 白名单成员     | 玩家唯一标识（UUID）命中白名单集合         |
| ② 高阶游戏模式   | 当前游戏模式为创造类（creative）           |
| ③ 管理权限       | 具备 2 级管理权限（hasPermissions(2)）     |

- **组合规则**：三条件逻辑 OR，命中任意一条即通过。
- **判定顺序**：先查白名单集合 → 再查创造模式 → 再查 2 级管理权限；任一命中即返回 true。
- **无效玩家排除**：玩家对象为空，或连接为空（`connection == null`，即伪造 / 自动化伪玩家）时一律判定为无权限。
- **鉴权失败策略**：静默失败，不输出任何文本、不中断流程，直接按「未启用」路径继续。
- **每效果入口二次鉴权（惰性吊销）**：每个特性效果入口在实际生效前，调用一次「激活态判定」；该判定若发现「特性开关为开 但 当前无权限」，则**立即将该特性的布尔开关置为关闭**并返回未激活，避免下个判定周期重复判断。覆盖的效果入口包括：
  - 无敌特性（防御面：攻击 / 受伤 / 死亡事件拦截 + 周期血量锚定；攻击面：主动攻击触发必杀链）。
  - 资源特性（物品抛出倍率复制生效前）。
  - 自由特性（周期补写飞行标志）。
  - 杀戮光环（周期扫描前）。
  - 资源增幅（增幅附魔模块的全局开关受同一权限模型约束，见增幅附魔模块）。

---

## 2. 玩家级状态持久化

### 2.1 设计思路

所有特性的开关状态与倍率参数属于「每位玩家各自的个人偏好」，需要在玩家退出游戏、死亡重生、切换维度后仍然保留，不能每次登录从零开始。

状态清单（玩家能力数据）：

| 字段             | 类型  | 默认值 | 说明                                                     |
| ---------------- | ----- | ------ | -------------------------------------------------------- |
| 无敌特性激活态   | bool  | false  | —                                                        |
| 生命锚定血量     | float | 20.0   | 无敌特性开启瞬间记录；允许上浮、不允许下探               |
| 资源特性激活态   | bool  | false  | —                                                        |
| 资源倍率         | int   | 10     | 下界约束由设置层夹到 ≥1                                  |
| 自由特性激活态   | bool  | false  | —                                                        |
| 杀戮光环激活态   | bool  | false  | —                                                        |
| 杀戮光环目标模式 | int   | 0      | 0 = 全部生物，1 = 仅敌对生物；非法值退化为默认模式       |

### 2.2 继承与恢复语义（必须满足）

- **死亡 → 复活**：通过「玩家克隆」事件完整继承所有状态（布尔开关、倍率、模式、锚定血量全部保留）。
- **切换维度 / 登录**：数据完整保留，并同步到客户端。
- **从存档加载**：先恢复持久化数据中的全部字段（含倍率 / 模式 / 锚定血量等非布尔偏好），**随后立即把 4 个布尔开关（无敌 / 资源 / 自由 / 杀戮光环）全部重置为关闭**，避免上次断开前的开启状态残留。
- **自由特性开关重置后**：若玩家处于创造或旁观类高阶模式，保留原版能力；否则强制清除「可飞行 / 飞行中」状态，避免飞行能力残留。

---

## 3. 控制通道（双通道等价）

### 3.1 设计思路

不同玩家习惯不同，提供两种操作通道，两种通道执行的语义完全等价，不出现「键位能开设置不能关」或反向的差异。唯一的行为差异：

- **快捷键按下**：视为反转当前布尔状态（toggle）。
- **精确设置**：视为强制设置到指定状态（set）。

### 3.2 快捷输入通道

每种特性绑定到独立按键；按下后的处理流程：

1. 客户端消费按键（consumeClick），不向其他系统继续传递。
2. 向服务端发送「请求切换某特性」的消息（仅传递特性唯一标识，不传具体结果）。
3. 服务端收到后：刷新状态容器 → 计算目标状态（toggle）→ 写入玩家状态 → 触发该特性的「开/关瞬间副作用」→ 给玩家发反馈并同步客户端。

四种特性的网络标识与默认按键：

| 特性     | networkId | 默认按键           |
| -------- | --------- | ------------------ |
| 无敌     | 1         | 小键盘 1（KP1）    |
| 资源     | 2         | 小键盘 2（KP2）    |
| 自由     | 3         | 小键盘 3（KP3）    |
| 杀戮光环 | 5         | 小键盘 4（KP4）    |

> 说明：杀戮光环的网络标识为 5（序号 4 预留），但客户端实际绑定到小键盘 4。

### 3.3 精确设置通道

通过命令 `/wenxingtools` 的子命令精确设置。命令树（前置权限：执行者需通过权限模型判定）：

| 子命令                                      | 语义                                                         |
| ------------------------------------------- | ------------------------------------------------------------ |
| `invincibleCharacteristics on/off`          | 设置无敌特性；开启瞬间记录锚定血量                           |
| `resourceCharacteristics on/off`            | 设置资源特性                                                 |
| `freedomCharacteristics on/off`             | 设置自由特性；开/关瞬间处理飞行标志                          |
| `killAuraCharacteristics on/off`            | 设置杀戮光环特性；反馈附当前模式名                           |
| `setKillAuraMode all/hostile`               | 设置杀戮光环目标模式                                         |
| `setResourceCharacteristics <amount>`       | 设置资源倍率（下界 1）                                       |
| `whitelist add/remove <名字或UUID>` / `list`| 白名单增删查                                                 |
| `cenchant <targets> <附魔ID> [等级]`        | 覆写式写入附魔到目标生物的主手物品                           |

设置执行流程：前置权限校验 → 执行体内部再鉴权一次（防御性编程）→ 计算目标状态 → 写入玩家状态 → 触发「开/关瞬间副作用或模式切换副作用」→ 反馈并同步。

---

## 4. 反馈展示规范

### 4.1 设计思路

采用**硬编码中文文案**。高频「切换 / 设置类」反馈一律放屏幕底部操作栏（actionbar），避免刷屏；「模式切换」「白名单列表」等管理 / 选择型反馈允许聊天主区输出。

| 场景                         | 位置                       | 内容格式                                                     |
| ---------------------------- | -------------------------- | ------------------------------------------------------------ |
| 普通特性开启成功             | 操作栏                     | `<特性名>：已开启`（绿色）；杀戮光环附「（当前模式: 模式名）」；资源特性附「（倍率: 数值）」 |
| 普通特性关闭成功             | 操作栏                     | `<特性名>：已关闭`（红色）                                   |
| 倍率设置成功                 | 操作栏                     | `资源特性倍率：已设置为 <数值>`（黄色）                      |
| 杀戮光环模式切换             | 聊天主区（仅执行者本人）   | `杀戮光环模式已切换：<模式名>`（青色）                       |
| 白名单 add                   | 聊天主区                   | 成功：`已将 <名字(UUID)> 加入白名单。`（绿色）；重复：`<display> 已在白名单中。`（红色） |
| 白名单 remove                | 聊天主区                   | 成功：`已将 <display> 从白名单移除。`（绿色）；不存在：`<display> 不在白名单中。`（红色）；非法入参：`白名单目标必须是在线玩家名或 UUID。` |
| 白名单 list                  | 聊天主区（仅执行者本人）   | 空：`当前白名单为空。`；非空：表头 `当前白名单：` + 逐行 `- 名字(UUID)` |
| 白名单被移除（在线玩家主动吊销） | 操作栏                     | `权限被撤销：所有特性已关闭。`（红色）                       |
| 权限不足                     | 静默，无任何输出           | —                                                            |

---

## 5. 特权特性的行为语义

### 5.1 无敌特性 —— 全伤害免疫（防御面 + 攻击面）

> 说明：本特性的激活态同时作用于「作为受害者」与「作为攻击者」两种角色。持有本特性的玩家既是「免伤的盾」，也是「主动攻击时对目标执行必杀链的矛」。

#### 5.1.1 防御面（作为受害者时全伤害免疫）

当玩家为受害者且激活态有效时：

- 攻击事件发起层：取消攻击事件（`LivingAttackEvent`）。
- 受伤结算层：取消受伤事件并将伤害量置 0（`LivingHurtEvent`）。
- 死亡事件触发层：取消死亡事件并无条件把血量回满 `setHealth(getMaxHealth())`（`LivingDeathEvent`）。
- 实体周期层：每游戏周期校验血量，若低于锚定值则回写到锚定值。
- 血量上限钳制：若最大生命值异常 ≤ 0，则恢复为安全下界 20，避免最大生命值为 0 导致锚定逻辑崩溃。

**锚定血量语义**：特性开启瞬间记录当前血量为锚定值；后续任何伤害都不能将血量拉低到锚定值以下；若开启后玩家通过药水等手段提升了血量，则锚定值自动上浮到新的更高值（只升不降）。开启 / 锚定由设置侧负责；周期侧只做「低于锚定值就回写、高于锚定值就上浮锚定」。

#### 5.1.2 攻击面（作为攻击者时触发必杀链）

当玩家为攻击者且激活态有效时，其发出的攻击会对目标触发「必杀链」（见 §5.5）。该必杀链对玩家目标与非玩家目标分开处理。

### 5.2 自由特性 —— 生存可飞行

- **行为**：向玩家能力中加入「可飞行」标志；针对持有者自身。
- **周期补写**：每游戏周期，若激活态有效则强制设置 `mayfly=true` 并刷新能力（仅在「当前不满足」或「与上次记录不一致」时写入，降低开销）。
- **关闭语义**：当关闭且原记录为开启时，若玩家既不是创造类也不是旁观类，则清除「可飞行 / 飞行中」；否则保留原版能力。
- **登出清理**：玩家登出时清掉其临时飞行状态记录，避免跨会话残留。

### 5.3 资源特性 —— 丢弃物品倍率复制

- **行为**：玩家扔出物品时（`ItemTossEvent`），若激活态有效且倍率 > 0，把该堆物品数量按倍率放大，并拆分到多堆落地。
- **拆分规则**：先计算 `最终数量 = 原数量 × 倍率`；原实体保留「不超过单堆上限」的一份；剩余数量按单堆上限逐批拆成多个新实体；新实体复制原实体的坐标与位移，设置拾取延迟并记录投掷者。
- **超堆拆分**：仅当放大后数量超过单堆上限才拆堆，避免超上限被丢弃。
- **边界**：倍率为 0 或负数时直接跳过（不放大）；最终数量计算出 0 时回退为原始数量。

### 5.4 杀戮光环特性 —— 玩家周围周期性范围灭杀

#### 5.4.1 触发时机与范围

- 每 20 个游戏周期扫描一次（节流），在服务端周期末尾相位执行；对所有维度所有在线玩家各做一次。
- **作用范围**：以玩家自身为中心、半径 5 格的正方形（按 X / Z 平面判定，|ΔX| ≤ 5 且 |ΔZ| ≤ 5，即直径 10 格）。
- **单次灭杀上限**：每次扫描最多处理 100 个目标，达到即停止本轮扫描。
- **前置检查**：特性激活态有效（二次鉴权通过）+ 玩家自身存活且未濒死；任一不满足则本周期跳过。

#### 5.4.2 目标过滤

通用过滤（两种模式均先通过）：目标非空且 ≠ 玩家自己；存活；非玩家；可被攻击（`isAttackable`）；|ΔX| ≤ 5 且 |ΔZ| ≤ 5；非装饰实体（排除盔甲架）。

| 模式                     | 附加条件                                                         |
| ------------------------ | ---------------------------------------------------------------- |
| 全部生物（默认，mode=0） | 通过通用过滤即可                                                 |
| 仅敌对生物（mode=1）     | 通用过滤 **AND**（具备敌对战语义 `Enemy` OR 所属分类为怪物分类 `MONSTER`） |

#### 5.4.3 灭杀执行体

复用「必杀链」（`forceKillEntityByCommandChain`），保证掉落 / 经验 / 最后攻击者标记 / 死亡统计等全部正常触发。

### 5.5 攻击特性 —— 必杀链（无敌特性攻击面共用）

> 说明：本模块不再单独设置独立「攻击特性」布尔开关；任何持有无敌特性的玩家，在其主动攻击 / 杀戮光环灭杀 / 伤害结算触发时，都走同一条必杀链。

#### 5.5.1 击杀链核心（两步式）

对单个目标按以下顺序执行，且重入保护开启（防止 `hurt()` 再次进入击杀流程导致递归）：

1. 清除目标防御类效果：伤害抗性、伤害吸收、生命恢复、防火。
2. 重置目标无敌帧与受伤计时（`invulnerableTime` / `hurtTime` 归零）。
3. 把目标血量压到低位：`setHealth(min(2, 最大生命值))`——先压血，保证随后 `hurt` 时目标仍存活并稳定触发一次死亡结算。
4. 再次重置无敌帧与受伤计时。
5. 以玩家作为伤害来源，向目标施加一笔极大数值伤害 `hurt(playerAttack, 1e9)`，让目标正常走完完整死亡流程（`die` → `dropAllDeathLoot`），保留玩家伤害来源、击杀归属与掉落物。

**顺序固定为「先 setHealth(2)、后 hurt(1e9)」**：目标仍存活时只触发一次 `die()`，避免旧三段式（hurt → setHealth → hurt）在首次 hurt 已致死时对死实体重复结算，造成重复死亡事件 / 重复掉落处理。

#### 5.5.2 保底兜底

当循环击杀后目标仍存活（极端防护），调用 `killByDeathFallback`：通过反射调用实体 `die(DamageSource)` 兜底；失败则记录警告日志，不中断流程。

#### 5.5.3 循环与终止

- 整体附带 10 次安全循环上限；每轮先检查目标是否存活，一旦死亡立即终止。
- 重入保护：重入标记为 `true` 时直接返回，防止嵌套调用无限递归 / 重复结算。

#### 5.5.4 前置检查与排除规则

- 目标必须存活（已死目标直接跳过）。
- 目标须在服务端环境中执行（客户端跳过）。
- 攻击来源必须是真实玩家对象（排除自动化伪玩家：连接为 null 的玩家）。
- **PVP 语义**：目标为玩家时，主动攻击链（`LivingAttackEvent` 触发）显式跳过；但伤害结算（`LivingDamageEvent` 触发）仍会对玩家目标执行单目标击杀链。受害者若本身开启无敌，则受害者侧的生命防御优先取消伤害，攻击链整体因伤害被取消而不再生效——即「受害者防御优先级高于攻击者必杀」。

#### 5.5.5 触发入口（激活态有效的攻击者）

- 攻击事件发起层（`LivingAttackEvent`@HIGH）：攻击者激活态有效 → 对非玩家目标执行必杀链（`forceKillEntityByCommandChain`）。
- 伤害结算层（`LivingDamageEvent`@LOWEST）：攻击者激活态有效 → 将目标承受伤害量直接放大到 `1e9`；若目标为玩家执行单目标击杀链（`applyDirectKill`），否则执行必杀链。
- 杀戮光环周期扫描：直接调用必杀链（`forceKillEntityByCommandChain`）。

> 说明：攻击增幅（增幅附魔模块）在「必杀链运行中」会自动让位——攻击增幅事件入口在最前端检查重入标记，为真时直接跳过，避免对同一目标重复补刀 / 重复触发死亡事件。

---

## 6. 白名单管理

### 6.1 设计思路

白名单是唯一能「跨游戏模式」给普通生存玩家赋权的手段；支持按唯一标识（离线玩家也能加）、按在线玩家两种入口，并便于管理员直接手工编辑。

- **数据形态**：UUID 字符串集合（天然去重）。
- **持久化**：JSON 文件 `wenxingtools_whitelist.json`，位于模组配置目录（config），全局配置级，与世界存档解耦——整个游戏实例所有世界共用同一份。
- **文件格式**：无注释头的 UUID 数组（美化格式化输出）。
- **并发与原子化写入约束（必须满足）**：
  - **读写互斥**：所有读 / 写入口统一由同一互斥对象串行化，防止内存集合竞态与 Windows 文件锁导致的半写问题。
  - **持久化写入原子性**：写入时先生成临时中间文件，全部写入成功后再通过「原子替换 + 覆盖已存在」语义替换目标文件；若中间任何一步失败，删除临时中间文件并保留旧文件，绝不留半写状态。
  - **热加载即时性**：通过设置入口执行 add / remove 时，内存修改成功后立刻同步写回磁盘；同时对当前仍在线的目标玩家立即执行主动吊销。
- **序列化容错**：
  - 文件不存在 → 不创建（保持空集合）。
  - 解析失败 / 语法错误 → 记录错误日志，清空内存集合继续运行，不中断加载流程。

### 6.2 三条管理语义

- **add**（入参：在线玩家名或 UUID）：已存在 → 失败反馈「已在白名单中」；新增成功 → 立刻同步写回文件 → 成功反馈「已加入白名单」。
- **remove**（入参：在线玩家名或 UUID）：不存在 → 失败反馈「不在白名单中」；移除成功 → 立刻同步写回文件，**并立即主动吊销该在线玩家的 4 个特性布尔开关**（无敌 / 资源 / 自由 / 杀戮光环），推送「权限被撤销：所有特性已关闭。」并同步客户端。
- **list**：空 → 「当前白名单为空。」；非空 → 表头 + 逐行；只给执行管理的人本人看，不广播。

### 6.3 管理前置权限

白名单增删查三条管理语义的前置权限：执行者需通过权限模型判定（三选一 OR）。

---

## 7. 模块职责划分（语义层，不对应具体文件）

| 职责                 | 语义说明                                                     |
| -------------------- | ------------------------------------------------------------ |
| 权限判定器           | 三选一 OR 判定（白名单 / 创造 / 2 级权限），暴露统一布尔入口；排除伪玩家（连接为 null）；提供「特性激活态判定 + 失败立即吊销开关」的组合入口。 |
| 玩家状态容器         | 各特性激活态、资源倍率、杀戮光环模式、生命锚定血量等字段 + 死亡/切维度/登录继承语义 + 读/写访问器 + 加载时布尔归零。 |
| 白名单存储           | UUID 集合 + JSON 文件 + 读写互斥锁 + 临时文件原子替换写入 + 修改立刻持久化 + remove 时在线玩家主动吊销。 |
| 无敌防御链           | 攻击/受伤/死亡事件拦截 + 周期血量锚定 + 血量上限钳制；每层执行前二次鉴权 + 失败吊销开关。 |
| 必杀链执行体         | 两步式（setHealth(2) → hurt(1e9)）+ 反射 die() 兜底 + 10 次循环 + 重入保护；非玩家目标完整链，玩家目标单目标击杀；伪玩家排除。 |
| 杀戮光环周期处理器   | 每 20 服务端周期扫描玩家半径 5 格正方形范围目标，最多 100 个，复用必杀链；扫描前二次鉴权 + 失败吊销开关。 |
| 资源倍率处理器       | 物品抛出事件中按倍率放大堆叠，超堆拆分多堆落地；生效前二次鉴权 + 失败吊销开关。 |
| 飞行能力恢复器       | 周期补写 mayfly + 关闭时清除（非高阶模式前提下）+ 登出清理临时状态。 |
| 精确设置入口         | 各特性 on/off、倍率设置、模式设置、白名单 add/remove/list、cenchant 子语义。 |
| 客户端输入处理       | 快捷键注册 + 按键消费 + 切换请求发送。                       |
| 服务端切换请求处理   | 接收切换请求 + toggle + 副作用 + 操作栏/聊天区反馈 + 客户端同步。 |
| 状态同步             | 登录/克隆/重生/切维度/设置/白名单吊销四类时机触发，把服务端状态同步到客户端（客户端仅展示，不决策）。 |

---

具体持久化载体 / 通信载体 / 注入技术 / 命令框架 / 客户端输入绑定方案均由实现者自由选择，本方案只定义语义层契约，不限制落地技术选型。

------



# 铁砧附魔增强 通用设计方案

> **定位**：本方案为独立模块的核心设计思路规格，仅定义功能语义与行为边界，不涉及任何具体实现技术。本模块可在任意版本项目中独立开发落地，不会对其他模块的开发造成任何干扰或依赖。

---

## 0. 设计范围与独立性声明

### 0.1 设计范围（本模块覆盖什么）

- 合并与附魔流程中的三类关键行为：「物品-附魔适配性」「附魔等级合并规则」「经验消耗与取出限制」。
- 与「其他模组已经通过更底层手段修改过合并流程」场景的双保险兼容设计。
- 与 Apotheosis（神化）模组的共存：让位「移除高等级取出限制」，由 Apotheosis 接管，二者协同不冲突。
- 多人联机语义：行为为硬编码固定语义，结果以服务端计算为准，经验扣除与物品变更都在服务端落地。

### 0.2 模块独立性声明

- 本模块是完全自包含的独立单元，不依赖任何其他模块的内部状态或私有接口。
- 本模块对外**不暴露**任何必须被其他模块调用的入口；所有改造仅作用于原版合并交互流程本身。
- 本模块不读写任何其他模块的持久化数据，不占用其他模块的键位、命令、标记位等资源。

---

## 1. 总体目标

- 在生存模式下，玩家打开原版合并界面即可享受改造后的行为；客户端 UI 不变，改造全部发生在后端逻辑层。
- 经验消耗固定为 1 级，同时移除原版的高等级上限限制，保证「高级附魔依然可以低等级取出」。
- 允许任意物品接受任意附魔。
- 兜底合并采用原版合并规则（同级 +1、异级取大、封顶附魔自身最大等级），不引入「等级直接相加」的自定义叠加，不覆盖其他模组已写入的附魔结果。
- 改造对所有玩家生效，不设权限门控、不做配置读取。

---

## 2. 实现行为（固定语义，非配置项）

> **当前实现说明**：本模块当前为**硬编码固定行为**（通过两个 Mixin 注入实现），不读取任何配置文件，也无外部权限判定。以下所有行为均为模块内写死的最终语义，不提供运行时切换开关。

| 行为              | 固定值                 | 说明                                                                            |
| ----------------- | ---------------------- | ------------------------------------------------------------------------------- |
| 任意附魔放行      | `true`                 | `AnvilMenuMixin` 的 `@Redirect canEnchant` 强制返回 true。                      |
| 固定经验          | `1`                    | `AnvilMenuFinalizerMixin` 在 `createResult` 返回后强制 `cost.set(1)`。           |
| 移除高等级取出上限 | `Integer.MAX_VALUE`    | `AnvilMenuMixin` 的 `@ModifyConstant`（40 → `Integer.MAX_VALUE`）；未安装 Apotheosis 时生效。 |
| 兜底合并规则      | 原版规则               | 同级 +1、异级取大、封顶附魔自身最大等级；仅当结果槽为空时触发，不覆盖正常结果。 |
| 注入优先级        | 999 / 500              | `AnvilMenuMixin`（任意附魔 + 移除上限）优先级 999；`AnvilMenuFinalizerMixin`（兜底合并 + 固定经验）优先级 500。 |

> **关键差异**：模块内通过优先级高低区分两个 Mixin——优先级 999 负责「前置放行 + 移除取出上限」（可能被同优先级或更高优先级顶掉），优先级 500 负责「出口兜底合并 + 固定经验」（在结果生成流程全部走完之后再执行一次，作为不依赖前置注入点的保底）。

---

## 3. 三类改造行为的详细语义

### 3.1 物品-附魔适配性（任意附魔贴任意物品）

- 行为：所有「该物品是否支持此附魔」的查询 → 强制返回 true。原版适配判定被永久放行（优先级 999，`@Redirect` `canEnchant`）。

- **双保险接管流程（核心设计思路，防止原版流程在前置判定就提前跳过）**：

  > 原版合并界面「附魔书 + 目标物品」的合并流程，存在多个「在调用适配判定之前就根据分类/分支判断提前跳过附魔书合并」的返回点。仅仅放行适配判定无法覆盖这些早期跳过点，会出现「附魔书放上去结果空」的现象。
  >
  > 因此任意附魔放行必须搭配「二次接管机制」，保证即便前置适配全部失效，兜底层至少命中一次：
  >
  > 1. **第一阶段（前置适配放行，优先级 999）**：对所有单点适配查询做放行（找不到就安静跳过，不要强制必须命中）。
  > 2. **第二阶段（结果槽空时兜底合并，优先级 500）**：在结果生成流程全部走完之后，再执行一次「兜底合并判定」：
  >    - 判定条件（全部同时满足）：左槽非空且不是附魔书、单格堆叠；右槽是附魔书；原版走完后结果槽依然为空。
  >    - 判定通过时，执行兜底合并算法：
  >      ① 从附魔书读取全部附魔条目；
  >      ② 对每一条附魔，若左槽副本中已存在同名附魔，则按 §3.2 原版合并规则计算新等级；若不存在同名附魔，则原样加入左槽副本的附魔集合；
  >      ③ 将合并后的附魔集合写回左槽副本；
  >      ④ 若左槽副本存在有效附魔，则将结果槽物品设为该左槽副本，并按 §3.3 将经验成本设为固定值 1。

### 3.2 附魔等级合并规则（兜底采用原版规则）

当兜底合并产生新的附魔等级时（第一槽已有的附魔 X 与附魔书提供的同名附魔 X）：

| 场景（附魔书对第一槽）       | 结果等级                                                     |
| ---------------------------- | ------------------------------------------------------------ |
| 附魔书等级 **==** 第一槽等级 | 第一槽等级 + 1（同级叠加升一级）                             |
| 附魔书等级 **≠** 第一槽等级  | 取二者较大者（异级取大）                                     |
| 最终封顶                     | 取上一行结果与附魔自身最大等级的较小者（不突破自身原版上限） |

- 说明：本模块不启用「等级直接相加（1+1）」的自定义叠加；兜底合并结果与原版合并流程的规则完全一致。

### 3.3 固定经验与去除高等级取出限制

- **固定经验**：当且仅当「当前结果槽已有结果」时，把最终经验成本直接设为 `1`。若结果槽为空，则不修改经验成本。
- **去除高等级取出限制**：把「结果槽是否可取出」的判定阈值放大到极高值（`Integer.MAX_VALUE`），避免「固定为 1 级却拿不出」的矛盾。
- **与 Apotheosis 共存**：未安装 Apotheosis 时，「移除取出上限」生效；安装 Apotheosis 时，该注入因优先级较低自动让位，由 Apotheosis 接管「移除取出上限」，本模块仅保留固定经验。

---

## 4. 跨模组双保险兼容设计（核心设计思路）

### 4.1 背景问题

部分大型模组通过「比常规注入更底层」的手段先把合并流程里的关键调用点替换掉了。如果改造「完全依赖于我能注入到某个具体调用点」——一旦被替换，就会因为零命中而抛出注入失败，游戏直接启动崩溃。

### 4.2 双保险设计总原则

> **第一层（具体调用点注入，优先级 999）**：对具体调用点的所有「可能名字候选」都尝试一遍；未命中 → 安静跳过，不抛启动崩溃。
> **第二层（返回值出口 / 最终写入出口，优先级 500）**：不依赖前面任何调用点形态，只对最终结果做「裁剪/回填」——永远是放大方向、不压小，保证即便第一层 0 命中也能拿到语义范围内的结果。

### 4.3 双保险清单（对应到上面三类改造）

| 改造项                | 第一层：基于调用点（优先级 999，可能被顶掉，命中则生效）       | 第二层：不依赖调用点的兜底（优先级 500，100% 生效）                  |
| --------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ① 任意附魔            | 改物品-附魔适配判定调用点（`@Redirect` `canEnchant`，找不到就跳过） | 结果槽最终附魔数据写入时不再按「物品是否支持」过滤；若结果槽空则触发 §3.1 兜底合并 |
| ② 合并规则            | 兜底合并写入（仅结果槽为空才写，不覆盖正常结果）             | 合并结果裁剪上限（封顶附魔自身最大等级）                     |
| ③ 固定经验 & 取出限制 | 这两处通常不与其他模组深度改造冲突，保留单实现即可           | 写入经验值出口再兜底一次，取出阈值放大到极高值               |

### 4.4 为什么第二层特别推荐「返回值出口」思路

- 返回值出口只关心「最后拿到多少」，不关心这个结果是经过了多少次变换、来自哪个调用点形态——从而天然抵抗「前面被改成别的样子」。
- 语义上做的是「裁剪/固定」而不是强赋越界：兜底合并只在结果槽为空时写入，绝不覆盖其它模组已写好的结果；固定经验只在「结果已存在」的前提下设置，避免对空结果做无意义赋值。

---

## 5. 多人联机 / 服务端一致性约束

- 所有改造只在服务端执行，客户端 UI 保持原版合并交互形态。
- 最终经验扣除、物品写入、结果槽生成都必须是服务端权威源。

---

## 6. 模块职责划分（语义层，不对应具体文件）

| 职责         | 语义说明                                                     |
| ------------ | ------------------------------------------------------------ |
| 任意附魔改造 | 任意附魔放行（`@Redirect` `canEnchant`，优先级 999）+ 双保险兜底合并（左槽副本 + 原版合并规则 + 结果回填，仅结果槽为空时写，优先级 500）。 |
| 合并规则兜底 | 只在结果槽为空时按原版规则合并（同级 +1 / 异级取大 / 封顶附魔自身最大等级），不引入自定义叠加。 |
| 经验改造     | 结果存在时成本固定为 1；取出阈值放大到极高值（`@ModifyConstant`）。 |
| 兼容性协调   | 前置放行注优先级 999、出口兜底注优先级 500，均低于 Apotheosis 的 1000，让其接管「移除取出上限」，避免抢占冲突。 |
| 结果槽最终归一 | 出口再兜底一次，确保多层改造结果最后收敛一致（防御多层实现遗漏）。 |

---

具体持久化载体 / 配置文件格式 / 注入技术 / 命令框架 / 通信载体均由实现者自由选择，本方案只定义语义层契约，不限制落地技术选型。





------



# 增幅附魔模块 通用设计方案

> **定位**：本方案为独立模块的核心设计思路规格，仅定义功能语义与行为边界，不涉及任何具体实现技术。本模块可在任意版本项目中独立开发落地，不会对其他模块的开发造成任何干扰或依赖。

---

## 0. 设计范围与独立性声明

### 0.1 设计范围（本模块覆盖什么）

- 四种自定义附魔的通用配置属性（最大等级、权重、适用范围、冲突规则、获取渠道）。
- 四种附魔的效果行为语义：触发时机、判定条件、数值公式、排除范围、溢出上限。
- 自定义附魔写入思路：特权命令绕过原版附魔校验时应采用的「覆写式写入策略」。
- 资源增幅效果的多重入口设计思路：为什么需要多条覆盖路径，以及各路径之间如何防重复处理。
- 命名空间约定原则：所有自定义标记必须带模组名前缀，避免与其他模组冲突。
- 专用配套工具：资源增幅镐的设计规格与行为语义。

### 0.2 模块独立性声明

- 本模块是完全自包含的独立单元，不依赖任何其他模块的内部状态或私有接口。
- 本模块对外暴露契约：四种附魔注册入口（由游戏加载机制托管）；一件专用配套工具的物品注册；资源增幅全局开关的切换入口（接收玩家触发的输入信号）；可选外部权限判定接口（传入玩家 → 返回布尔，未接入时默认允许，不造成硬依赖）。
- 本模块不对外暴露任何必须接入的强制入口；仅修改原版既有流程，不新增需要第三方主动调用的契约。
- 本模块不读写其他模块的任何持久化数据，不占用其他模块的任何键位、命令、标记位等资源。

---

## 1. 通用配置（四种附魔共享）

| 属性             | 值                                                           | 说明                                                         |
| ---------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 最大等级         | 5                                                            | 四种附魔在常规附魔获取渠道中能获得的最高等级；特权写入或合并叠加可更高，不被此上限限制。 |
| 稀有度           | COMMON（与原版普通稀有度等价）                               | 随机出现的权重；四个附魔使用相同稀有度保证出现概率均等。     |
| 附魔台成本范围   | 最低 10 级 / 最高 15 级（等级 1-5 附魔都落在同一窗口内）     | 附魔台成本。                                                 |
| 冲突集合         | 空                                                           | 四种自定义附魔互不冲突，允许在同一物品上同时存在。           |
| 获取渠道         | 全部加入「附魔台可出、村民可交易、战利品可出、非宝藏附魔」四类原版渠道 | 确保四种附魔都能通过常规途径获取。                           |

> 说明：四个附魔均为独立 `Enchantment` 子类，未显式声明冲突集合（默认无冲突）；其中「生命增幅」直接继承 `Enchantment`，「攻击增幅」「资源增幅」「药水增幅」继承共同的增幅基类，它们在该基类中统一了最大等级（5）与成本/获取规则。

### 1.1 特权命令附魔写入思路

**设计思路**：直接对「物品的附魔数据容器」做覆写式 set，而不是走原版的追加附魔流程。
**原因**：原版追加流程会做「物品支持此附魔吗？冲突检查？等级上限检查？」等校验，特权命令就是要绕过这些限制；同时直接操作容器能保证「同一附魔已存在就覆盖为新等级、不存在就新增」，天然避免「同一物品出现两条同附魔不同等级」的显示缺陷。

- 推荐策略：取出当前物品的附魔容器 → 转可变容器 → 在可变容器上对目标附魔执行 set（set 语义是「有则覆盖，无则新增」）→ 把结果容器写回物品。
- 写入范围：命令给定的每一个目标实体的「主手手持物品」（没拿东西就静默跳过）。
- 等级为 0 时移除该附魔条目。

### 1.2 参数常量（固定值）

四种增幅附魔的所有数值参数使用固定常量，不根据已加载模组做动态切换。

| 附魔     | 参数项                  | 数值                                                         |
| -------- | ----------------------- | ------------------------------------------------------------ |
| 生命增幅 | 每级减伤比例            | 1% / 级                                                      |
| 生命增幅 | 幸运值加成              | 每件带生命增幅护甲 +1,024（按件数缩放；4 件全满 = +4,096；与等级无关） |
| 生命增幅 | 减伤上限                | 100%（等级总和 × 1% 超过 100% 时封顶为 100%，即无伤害）      |
| 攻击增幅 | 每级真伤比例 / 吸血比例 | 1% / 级（真伤与吸血同比例）                                  |
| 资源增幅 | 可堆叠因子              | 等级 × 100（对所有掉落物统一使用，不区分可堆叠/不可堆叠）    |
| 资源增幅 | 生物经验额外倍率        | ×100（固定值，与等级相乘）；生物经验新值 = 原值 × 等级×100 × 100，夹到 20 亿 |
| 资源增幅 | 生物击杀掉落物倍率      | 直接并入原掉落品（`original.grow(original.getCount() × 因子)`），不生成新实体；总份数 = 原始份数 × (等级×100 + 1) |
| 资源增幅 | 方块/作物收获掉落倍率   | 额外生成一份独立堆（`drop.getCount() × (因子 + 1)`）；总倍率 = 等级×100 + 2 份 |
| 药水增幅 | 每级时长                | +1 小时 / 级（72,000 tick / 级）                             |
| 药水增幅 | 每级提级                | +1 级 / 级（仅对玩家身上已存在的正面效果；等级放大器上限 255，到达后仅续时不提级） |
| 药水增幅 | 时长上限                | `Integer.MAX_VALUE - 72000`，避免多次累计溢出为负数导致效果到期 |

---

## 2. 生命增幅

### 2.1 基础属性

| 属性                 | 值                                                           |
| -------------------- | ------------------------------------------------------------ |
| 适用物品             | 全护甲类（头部 / 胸部 / 腿部 / 足部装备）                    |
| 适用槽位             | 四件护甲槽（头部 / 胸部 / 腿部 / 足部）                      |
| **生效判定槽位规则** | **仅读取四个护甲槽上的生命增幅等级并求和**（不读取主手、副手、其他装备槽） |

### 2.2 等级计算规则

- **是否生效（生效门槛）**：玩家四件护甲**至少任意一件**附有生命增幅 → 视为「穿上了生命增幅装备」。
- **伤害减免比例（数值计算）**：玩家四件护甲上生命增幅的**等级总和** × 每级比例（1%）。
- **等级累计路径**：仅对「四件护甲槽」的物品求和；主手/副手/背包/饰品槽等其他槽位即使附了生命增幅，也不计入任何效果。

### 2.3 四大效果（任意一件护甲生效就全部同时生效）

**① 伤害豁免（减伤）**

- 触发时机：玩家作为受害者的伤害计算阶段。实际实现：`LivingDamageEvent` @ `EventPriority.HIGH`（仅当受害者是玩家时读取四护甲总等级）。
- 公式：`最终伤害 = 原始伤害 × (1.0 - 四件总等级 × 每级比例)`；结果直接写入事件伤害量。
- **减伤封顶**：`四件总等级 × 每级比例` 超过 100% 时封顶为 100%（`reduction` 钳制到 1.0），即玩家最高可完全免疫伤害（无伤害），避免减伤比例过高导致结果为负值、行为不可预期。

**② 幸运值提升（属性修饰符挂到玩家）**

- 数值：**每带一件生命增幅护甲 +1,024**，按「当前带生命增幅的护甲件数」缩放；4 件全满 = +4,096（与等级无关，只看是否有该附魔）。
- 方式：给玩家 Luck 属性挂一个永久修饰符；任意一件有生命增幅就挂，全部护甲都没了就移除；件数变化时更新修饰符数值。

**③ 所有装备耐久不消耗（无法破坏效果）**

- 判定条件：只要玩家四件护甲中「任意一件有生命增幅」 → 玩家身上所有装备（所有槽位）在周期判定中把每件装备的耐久值直接重置为 0（`setDamageValue(0)`）。
- 范围：不局限于四件护甲，而是「玩家全身装备耐久全部不扣」。

**④ 无限夜视（药水效果）**

- 应用时机：玩家周期判定中，**仅当「玩家穿有至少任意一件生命增幅护甲」**才挂长时效夜视效果。
- 应用方式：应用一次夜视效果，持续时间设为近乎永久（`Integer.MAX_VALUE`），不显示粒子、不显示图标气泡；仅在「当前没有夜视」或「当前剩余时长 ≤ 200 tick」时才重新施加。
- **移除时细节**：当玩家不再有生命增幅、要移除夜视时，直接移除夜视效果并同时移除可能的幸运值药水效果。

### 2.4 生效检测建议

- 长时效效果（幸运修饰符、夜视、耐久重置）：放在「每玩家每周期」做一次即可（开销不大）。
- **仅变化时重刷**：用「上次生效的总等级」做缓存标记，总等级未变化时直接跳过 apply/clear；非增幅玩家首 tick 也跳过，避免每个玩家每 tick 反复执行清除逻辑。仅当玩家此前处于生命增幅状态、本次总等级归零时才执行清理，避免误删普通夜视 / 幸运药水效果。
- 减伤：直接挂伤害计算阶段即时钩子。

---

## 3. 攻击增幅

### 3.1 基础属性

| 属性                 | 值                                                           |
| -------------------- | ------------------------------------------------------------ |
| 适用物品             | 原版武器类（剑、斧、三叉戟等主手武器分类） + 枪械类 + 弓类（原版弓/弩、模组弓、`#c:bows` 标签弓） |
| 适用槽位             | 主手                                                         |
| **生效判定槽位规则** | **对玩家全部装备槽的攻击增幅等级求和**（不限于主手；作为「以玩家为增幅主体」的全局等级） |

> **关键差异**：攻击增幅不以「单件主手武器」为增幅来源，而是**以玩家为增幅主体**——每游戏周期遍历玩家全部装备槽（头、胸、腿、足、主手、副手、衣饰、背包）的攻击增幅等级并求和，将总和写入玩家持久化数据；伤害结算时直接读取该缓存值。这样近战 / 远程 / 枪械 / 魔法等所有玩家发起的伤害都能覆盖到，而不再依赖当前主手或子弹 NBT。

### 3.1.1 物品-附魔适配范围扩展

原版武器分类仅包含一小部分武器物品。为了让更多自定义/模组物品能在附魔台、合并流程中原生接受攻击增幅，需要对适配判定做「OR 放行」扩展：

- **扩展规则**：`返回值 = 原版适配判定 OR 该物品属于「扩展物品集合」`；扩展放行不覆盖原版集合，二者是并集关系。
- **扩展物品集合**：枪械类物品（通过 `IGun` 接口识别，模组未加载时透明跳过）、弓类（`BowItem` / `CrossbowItem` / `#c:bows` 标签）。

### 3.2 等级计算

- 以玩家为增幅主体，每游戏周期对所有装备槽的攻击增幅等级求和，写入玩家持久化数据（仅在等级变化时写入，降低 NBT 写入开销）。

### 3.3 效果触发与细节

#### 触发条件（缺一不触发）

- 服务端执行（客户端跳过，避免双扣）。
- 伤害源实体为真实玩家：排除自动化伪玩家（连接为 null）。
- 必杀链运行中时直接跳过（避免与无敌特性必杀链对同一目标重复结算）。
- 目标活着（已死目标直接跳过）。

#### 行为（按顺序执行，分两个分支）

**分支 A：目标当前血量 > 真伤阈值（常规非致死分支）**

- 计算真伤阈值：`真伤阈值 = 目标最大生命值 × (攻击增幅等级 × 每级比例)`（1% / 级）。
- 执行真伤扣血：`剩余血量 = 目标当前血量 - 真伤阈值`，直接设置剩余血量（剩余血量 > 0，不会致死）。
- 执行等量吸血：攻击者回复与真伤值等量的血量，回复量自动夹到攻击者自身最大生命值。

**分支 B：目标当前血量 ≤ 真伤阈值（溢出致死分支）**

- 第一步：清除目标身上的防御类效果（伤害抗性 / 伤害吸收 / 生命恢复 / 防火），避免它们抵消结算伤害。
- 第二步：重置目标的无敌帧与受伤计时（`invulnerableTime` / `hurtTime` 归零），确保结算伤害稳定穿透补刀。
- 第三步：以玩家作为伤害来源，向目标施加一笔极大数值伤害（1e9），确保目标正常走完完整死亡流程（死亡统计/掉落/死亡事件全部触发），并刻意保留「玩家攻击（playerAttack）」这一伤害来源，使掉落 / 经验 / 最后攻击者标记 / 死亡统计能正确归属。
- 第四步：计算溢出吸血（leftover 吸血）——真伤阈值中「超出当前血量」的多余部分，返还给玩家做等量吸血；吸血量自动夹到攻击者自身最大生命值。

#### 重入保护

- 分支 B 的 `hurt(1e9)` 会再次派发伤害事件并重入本方法；因此整个结算被包在「重入保护标记」中，为真时直接返回，避免无限递归（栈溢出）。

#### 阶段建议（实际实现优先级值）

- **真伤逻辑**：挂伤害流程的最前阶段（`LivingHurtEvent` @ `EventPriority.HIGHEST`），早于原版减伤阶段。
- **生命增幅减伤逻辑**：挂伤害流程较后阶段（`LivingDamageEvent` @ `EventPriority.HIGH`），与真伤阶段分离，避免互相干扰。

---

## 4. 资源增幅

### 4.1 基础属性

| 属性                 | 值                                                           |
| -------------------- | ------------------------------------------------------------ |
| 适用物品             | 原版挖掘类（镐 / 铲 / 斧 / 锄）+ 枪械类 + 带跨模组通用工具标签（`#c:tools`）+ 弓类 |
| 适用槽位             | 主手                                                         |
| **生效判定槽位规则** | **仅读取主手持物品上的资源增幅等级**（破坏方块/生物击杀时主手拿着什么就用什么的等级） |

### 4.1.1 物品-附魔适配范围扩展（多通路并集）

资源增幅的适配判定采用「多路 OR 并集」：

1. **原版通路（挖掘工具分类）**：原版适配判定 — 镐/铲/斧/锄等原版挖掘工具。
2. **枪械通路**：`IGun` 接口物品（模组未加载时透明跳过）。
3. **标签通路（跨模组通用工具）**：物品带 `#c:tools` 标签。
4. **弓类通路**：`BowItem` / `CrossbowItem` / `#c:bows` 标签。

**最终判定**：四路 OR，任一命中即视为该物品原生支持资源增幅。

### 4.2 统一放大公式

- **统一可堆叠因子**：`因子 = 等级 × 100`（对所有掉落物统一使用，不区分是否可堆叠）。
- **掉落物倍率（分两类，实现不同）**：
  - **生物击杀掉落**：直接用 `original.grow(original.getCount() × 因子)` 把额外数量并入原掉落品本身，不生成新实体；总份数 = 原始份数 × (因子 + 1)。（生物掉落此路径不拆分超堆、不单独处理不可堆叠分支。）
  - **方块破坏 / 作物收获掉落**：额外生成一份独立堆，`额外份数 = 原始份数 × (因子 + 1)`；总倍率 = 因子 + 2 份。
- **生物经验公式**：`新经验 = 原始经验 × 因子 × 100`；经验作为数量型资源，统一使用可堆叠类型因子。
  - 经验硬上限：20 亿（夹取，避免整数溢出 / 经验球爆服）。
- **生效前提**：全局开关开启（见 §6）。关闭时等级强制归零。

### 4.3 效果触发的多重入口设计（实际落地覆盖路径）

> **核心问题**：游戏中「产生掉落 / 产生经验」的入口不止一条。只挂一个钩子的话，总有路径漏掉。所以设计目标必须覆盖多类路径并配套「防重复处理标记」避免放大两次。

#### 实际落地覆盖的两条路径

**路径一：生物类（击杀掉落 + 经验）**

- 覆盖：生物击杀掉落流程 + 生物经验流程。
- 防重复：使用线程级集合标记。同线程同实体在同一次处理周期内仅放大一次，处理完成后立即移除标记；当集合过大时主动清空，防止长时间运行内存膨胀。

**路径二：方块/作物类（破坏掉落 + 右键收获）**

- 覆盖：方块破坏掉落流程 + 作物右键收获流程。
- 方块破坏前提：主手带资源增幅工具 + 方块可被采集。
- 作物右键收获触发条件（缺一不触发）：
  1. 玩家主手手持带资源增幅的工具；
  2. 目标方块状态中存在「生长阶段」整数属性；
  3. 当前生长阶段达到最大值；
  4. 交互动作符合收获行为。
- 作物右键收获执行流程：
  1. 生成该方块在当前状态下的原始掉落物；
  2. 走放大算法放大（额外生成独立堆）；
  3. 放大后物品以物品实体形式从方块中心位置弹出；
  4. 将方块的生长阶段重置为「该属性枚举的最小值」（通过枚举最小值获取，不硬编码 0，兼容自定义作物）；
  5. 取消本次右键交互的默认行为。

#### 远程武器类子弹的附魔等级读取（可选接口适配）

- 以抽象函数形式提供：`传入子弹实体 → 返回等级 0~N`。
- 通过子弹 NBT 记录资源增幅等级（在子弹生成时把枪械附魔等级写入子弹持久化数据）；被适配模组不存在时返回 0，完全透明，硬依赖解除。

---

## 5. 药水增幅

### 5.1 基础属性

| 属性                 | 值                                                           |
| -------------------- | ------------------------------------------------------------ |
| 适用物品             | 全护甲类（头部 / 胸部 / 腿部 / 足部装备）                    |
| 适用槽位             | 四件护甲槽（头部 / 胸部 / 腿部 / 足部）                      |
| **生效判定槽位规则** | **仅读取四个护甲槽上的药水增幅等级**（不读取主手、副手、其他装备槽） |

### 5.2 触发时机与去重

- 药水增幅**不是周期性效果**，而是在「护甲槽位上出现一件带药水增幅的装备」的那一刻结算一次；脱下并不减少已累加的时长。
- **去重规则**：每玩家每护甲槽记录「上次触发时所穿上的带增幅装备」。同一件装备重复 `set`（同槽位重复穿戴）不重复触发；只有「换装 / 脱下重穿」到一件与上次不同的带增幅装备时才重新触发一次。
- **结算等级**：取当前护甲四件中「药水增幅等级最高」的那一件作为本次结算等级（不是求和；槽位有增幅即触发）。
- **触发入口**：装备变化事件（`LivingEquipmentChangeEvent`）；客户端跳过（仅服务端结算）、防重入保护（避免 addEffect 重派状态效果时再次进入本模块）。

### 5.3 效果语义（仅对已存在的正面效果生效）

- **只对玩家身上「已存在的正面药水效果」生效**：不新增、不强加玩家本不具备的效果。
- 对每个正面药水效果：
  - **时长**：`新时长 = 原时长 + 结算等级 × 1 小时`（每级 +72,000 tick）；并夹取到安全上限（`Integer.MAX_VALUE - 72000`），避免多次累计溢出为负数导致效果提前到期。
  - **等级**：`新等级放大器 = 原放大器 + 结算等级`；放大器上限 255（超过后显示 / 结算异常），到达上限后仅续时、不再提级。
- **脱下不减时长**：脱下或换为无增幅护甲时，已累加的时长 / 等级保持不变，仅更新该槽位的「上次触发装备」记忆标记。
- **负向效果不处理**：仅辨认 `isBeneficial()` 为真的正面效果。

### 5.4 状态清理

- 玩家登出时清空其「每护甲槽上次触发装备」的记忆，避免内存泄漏与跨会话误判。

---

## 6. 资源增幅全局开关机制（唯一切换入口）

> **核心原则**：整个游戏内「资源增幅附魔是否产生倍率效果」只有**一个布尔状态**，由玩家持久化数据中的本模块专用开关位唯一表示（默认 false，即默认关闭）；**唯一的切换入口**是「Shift+右键 手持任意带资源增幅附魔的物品（含资源增幅专用镐）」。切换后，该玩家身上所有带资源增幅附魔的物品统一生效/失效，是玩家级全局开关，不是单物品级开关。

### 6.1 切换入口

- **触发方式**：玩家手持带资源增幅的手持物品（或资源增幅专用镐），按下 Shift 并右键 → 切换玩家级全局开关，立即生效。
- **反馈文案（操作栏展示，格式固定）**：
  - **开启时**：整句绿色；若当前物品资源增幅等级 > 0，文案为「资源增幅附魔：已开启（当前资源倍率: X，经验倍率: Y）」（X = 等级×100 + 2，Y = 等级×100 × 100）；否则为「资源增幅附魔：已开启」。
  - **关闭时**：整句红色，文案为「资源增幅附魔：已关闭」。
- **切换冷却防重复触发**：两次切换之间必须间隔至少 250ms，避免快速连点多次翻转导致刷屏。

### 6.2 Tooltip 引导文案

资源增幅专用镐的 Tooltip 底部额外附加一行灰色引导文案：`[Shift + 右键] 切换资源增幅附魔`；灰色字体的层级低于附魔列表，不喧宾夺主。

### 6.3 开关关闭时的精确行为语义（「资源归零，其他照常」）

关闭开关的目的是「临时停掉资源倍率这一个强玩法副作用」。效率、耐久、攻击伤害/速度这些工具本身的基础能力，在关闭后应完全保留。

- **归零的部分**（只要开关关闭，就像物品上根本不存在资源增幅附魔）：生物掉落物倍率、方块破坏掉落物倍率、右键收获成熟作物时的掉落倍率放大效果、生物击杀经验倍率。
- **不受影响、照常生效的部分**：最快挖掘效率、高等级耐久、攻击伤害 +100 / 攻击速度 +10 的两条显式修饰符、合并叠加的其他附魔、挖掘等级、Epic 稀有度、抗火、单格 1 堆叠等静态规格。

> 说明：开关关闭后，读取资源增幅等级统一返回 0，从而所有放大路径（生物掉落 / 经验 / 方块 / 作物）都自然不再放大。

### 6.4 开关状态默认值与初始化时机

- **默认值**：开关位未写入过 → 返回 false（默认关闭）。
- **用户关闭后，不允许任何逻辑自动把它重新开回去**：绝不能在玩家周期、背包周期等周期性轮询里把开关重新置为 true。
- **工具获取方式（合成/创造取出/命令给予）都不主动改开关位**：只负责写入三件套内置附魔，不主动修改开关位；开关位只在玩家执行 Shift+右键 切换动作时改变。

---

## 7. 资源增幅专用镐（配套工具）通用设计思路

> **定位**：作为「资源增幅附魔」的原生配套专用工具而存在，不是普通镐的皮肤；核心价值在于：① 物品层面自带「资源增幅满级 + 最快挖掘效率 + 耐久」等全套默认属性；② 物品层面对攻击伤害/攻速做显式加法加成；③ 为「资源增幅全局开关」提供独立专属切换入口。

### 7.1 外观与稀有度

- **外观**：工具模型采用下界合金镐外形（`Tiers.NETHERITE`），稀有度为 Epic（紫字）。
- **其他规格**：单格最大堆叠为 1；抗火（掉入岩浆/被火烧不会消失）。

### 7.2 合成配方

- **材料**：3 个铁锭（`forge:ingots/iron` 标签）+ 2 个任意原木（`minecraft:logs` 标签）。
- **形状**：标准镐形（上排三格为材料，中中与下中为手柄）。
- **合成产出**：1 把「资源增幅专用镐」，合成完成的同时自动写入内置附魔，不主动修改开关状态。

### 7.3 内置附魔与无法破坏

- **强制内置附魔（合成完成 / 创造模式取出时自动写入三件套）**：
  1. 资源增幅满级（核心定位附魔，取「当前等级 与 5」的较大值，不降级）；
  2. 高等级耐久（`UNBREAKING` 3，若不存在才写入）；
  3. 最快挖掘效率（`BLOCK_EFFICIENCY` 5，若不存在才写入）。
- **写入策略**：资源增幅执行取较大值写入；效率 / 耐久执行「若不存在则写入」。写入入口覆盖「合成回调」「默认实例（创造取出）」两处。
- **无法破坏（不掉耐久）的实现方式**：重写 `hurtEnemy` / `mineBlock`，把耐久损耗显式传 0（等价于空操作）。

### 7.4 挖掘等级与挖掘兼容性

- **挖掘等级**：最高级（下界合金，可挖黑曜石、远古残骸等）。
- **挖掘判定兼容**：加入「可被镐挖掘」的原版标签判定（`MINEABLE_WITH_PICKAXE`），让所有「仅镐能采」的判定都把它当作一把镐。

### 7.5 战斗属性（显式修饰符，去除隐式叠加）

为了数值完全可预测，本工具采用：

- **彻底舍弃品级隐式加成**：在获取默认属性修饰符时，过滤掉原版品级已挂上去的攻击伤害 / 攻击速度修饰符。
- **只挂显式两条修饰符（都是加法运算）**：
  1. 主手攻击伤害修饰符 = **+100**。
  2. 主手攻击速度修饰符 = **+10**。
- **修饰符作用槽位严格限定为主手**；副手拿着本工具不会加攻击属性。

### 7.6 附魔兼容性（全面放开）

- **附魔台适配**：`canApplyAtEnchantingTable` 返回「原版工具附魔分类可附」OR「资源增幅附魔本身可附」，从而放行所有工具类附魔 + 资源增幅本身。
- **合并适配（附魔书可合并 + 物品可附魔）**：`isBookEnchantable` 和 `isEnchantable` 全部返回 true。

### 7.7 合成/创造/命令给予一致性

三种获得方式都保证：① 内置附魔三件套已写入（资源增幅取较大值防降级，效率/耐久缺省写入）；② 不主动覆写全局开关。

---

## 8. 模块职责划分（语义层，不对应具体文件）

| 职责                 | 语义说明                                                     |
| -------------------- | ------------------------------------------------------------ |
| 附魔资源定义         | 4 份附魔定义（最大等级 = 5，稀有度 COMMON，成本 10/15，四类原版渠道）。 |
| 附魔常量中心         | 固定值常量（每级减伤 1% / 幸运值每件 +1024（4 件全满 = +4096）/ 减伤上限 100% / 每级真伤 1% / 可堆叠因子 = 等级×100 / 经验额外倍率 ×100 / 生物掉落总份数 = 原始 ×(因子+1) / 方块作物掉落总倍率 = 因子 + 2 / 药水增幅每级 +1 小时（72,000 tick）+ 每级 +1 级（放大器上限 255，到达后仅续时不提级）。 |
| 生命增幅效果挂接     | 每玩家周期（幸运/夜视/耐久重置，夜视仅当至少一件护甲生效时才挂；移除时直接移除夜视与幸运药水效果；用「上次总等级」缓存标记，仅在等级变化才重刷，非增幅玩家首 tick 跳过，仅从增幅态归零时才清理避免误删普通夜视/幸运） + 伤害流程较后阶段（`LivingDamageEvent`@HIGH，减伤，比例 ≥100% 封顶为 100%）。 |
| 攻击增幅效果挂接     | 每玩家周期汇总全装备槽等级写入持久化；伤害最前阶段高优先级（`LivingHurtEvent`@HIGHEST）：先算真伤阈值；目标血量 > 阈值 → 真伤扣血 + 等量吸血；目标血量 ≤ 阈值 → 清除防御类效果 + 重置无敌/受伤帧 + 玩家来源极大伤害（1e9）走完整死亡流程 + 溢出 leftover 吸血；重入保护；自动化伪玩家一律不触发；必杀链运行中自动让位；阶段与生命增幅减伤分离。 |
| 药水增幅效果挂接     | 护甲装备变化事件（`LivingEquipmentChangeEvent`）结算一次：槽位有增幅即触发；按「每护甲槽上次触发装备」去重（同一件重复 set 不触发，换装/脱下重穿才触发）；结算等级取四护甲中最高等级单件；只对玩家已存在的正面效果生效——每级 +1 小时、每级 +1 级（放大器上限 255，到达后仅续时）；时长夹取到安全上限；脱下不减时长；登出清理该玩家记忆。 |
| 资源增幅效果挂接     | 两条覆盖路径：① 生物击杀掉落 + 生物经验；② 方块破坏掉落 + 作物右键收获；两条都受全局开关守卫，关闭则等级强制归零；防重复处理使用线程级集合标记；生物掉落并入原掉落品，方块/作物收获额外生成独立堆。 |
| 资源增幅全局开关     | 单一布尔状态（玩家持久化数据专用位，默认 false）+ 唯一切换入口（Shift+右键 手持任意带资源增幅物品） + 250ms 冷却 + 统一绿/红文案；Tooltip 灰色引导文案。 |
| 掉落/经验工具类      | 统一公式纯函数：可堆叠因子 = 等级×100；生物掉落 = `original.grow(原始数量 × 因子)` 并入原掉落品（总 = 原始 × (因子+1)）；方块/作物掉落额外份数 = 原始 × (因子 + 1)，总倍率 = 因子 + 2；新经验 = 原始 × 等级×100 × 100 夹 20 亿。 |
| 资源增幅专用镐       | 定义工具规格：3 铁锭 + 2 原木合成；下界合金镐外观、Epic 稀有度、抗火、单格 1 堆叠；内置附魔写入策略；不掉耐久；两条显式战斗属性修饰符 +100/+10（先过滤原版隐式叠加）；附魔台 + 合并全开放兼容；最高级挖掘等级 + 可被镐挖掘标签；全局开关切换入口 + Tooltip 灰色引导。 |
| 命名空间前缀约定     | 所有自定义标记、修饰符 ID、资源路径、玩家持久化数据键 —— 全部带模组名前缀（如 `wenxingtools:resource_amp_enabled`、`wenxingtools:attack_amp_level`、`wenxingtools:resource_amplification_level`）。 |
| 特权命令附魔写入策略 | 命令入口（独立权限） → 直接对附魔数据容器做 set（覆盖/新增，不重复追加；主手物品；等级 0 移除）。 |
| 可选外部接口适配     | 攻击/资源增幅的附魔物品扩展集合、远程武器类子弹的附魔等级读取（抽象函数：传入子弹实体 → 返回等级 0~N；被适配模组不存在时返回 0，透明无硬依赖）；可选的权限判定接口（传入玩家 → 返回布尔，未接入时默认允许，不造成硬依赖）。 |

---

具体持久化载体 / 通信载体 / 注入技术 / 命令框架 / 客户端键位绑定方案均由实现者自由选择，本方案只定义语义层契约，不限制落地技术选型。
